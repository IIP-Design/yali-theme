<nav class="nav ui bottom fixed menu">
	<div class="nav_wrapper">
		<div>
			<a href='{{ site.url }}' class="nav_siteurl">
				<div class="img_placeholder">
					<img src="{{ site.theme_url }}/assets/img/yali_logo_mark.svg">
				</div>
				<span href="/yalinav">Young African Leaders Initiative</span>
			</a>
		</div>
		<div class="nav_list">
		{% if menu %}
			<div class="ui grid">
				{% for item in menu %}
			     <div class="ui item">
			       <a target="{{ item.target }}" href="{{ item.url }}">{{ item.title }}</a>
			     </div>
				{% endfor %}
			</div>
		{% endif %}
		</div>
		<hr>
		<div class="ui grid">
			<div class="img_footer">
				<img src="{{ site.theme_url }}/assets/img/us_flag_greyscale.svg">
			</div>
			<div><p>U.S. Department of State | Bureau of International Information Programs</p>
			</div>
		</div>
		<div>
			<p>This site is managed by the U.S. Department of State. Links to external sites should not be construed as an endorsement of the views or privacy policies expressed on those sites.</p>
		</div>
	 </div><!-- nav_wrapper -->
</nav>
