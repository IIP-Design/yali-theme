{% extends 'base.twig' %}

{% block content %}	
<section class="hero">
    <img class="ui fluid image" src="{{ header_url }}" srcset="{{ srcset }}" sizes="{{ size }}" alt="{{ header_url }}">
</section>
<section class="ui container title-overlay higher">
	<h2 class='title-overlay_title'>{{ page_data.title.rendered }}</h2>
	<div class='inner-container'>
		<section class='section mwf_intro'></section>

		<!-- CONTENT
			Hear From Fellows Content - Temp, will be added via editor as content block 
		-->

		<section class='section application'></section>

		<!-- CONTENT
			Application Resources
		-->

		<section class='addtl_content'></section>

	</div>
</section>
{% endblock %}